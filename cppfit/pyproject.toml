[build-system]
requires = ["setuptools>=64", "wheel", "pybind11>=2.10"]
build-backend = "setuptools.build_meta"

[project]
name = "trackfit_cpp"
version = "0.1.0"
description = "C++ track fit core with RT LUT"
requires-python = ">=3.9"
